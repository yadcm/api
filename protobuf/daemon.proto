syntax = "proto3";

package daemon;
option go_package = "protocol/daemon";

import "local.proto";
import "remote.proto";

service InternalAPI {
    rpc Online (local.ClientInfo) returns (remote.DaemonInfo);
}

service ExternalAPI {
    rpc Handshake(remote.HandshakeInfo) returns (remote.HandshakeInfo);
}

