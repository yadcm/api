syntax = "proto3";

package daemon;
option go_package = "protocol/daemon";

import "internal.proto";
import "external.proto";

service InternalAPI {
    rpc Online (internal.ClientInfo) returns (external.DaemonInfo);
}

service ExternalAPI {
    rpc Handshake(external.HandshakeInfo) returns (external.HandshakeInfo);
}

